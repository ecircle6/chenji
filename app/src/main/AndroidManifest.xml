<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- 精确闹钟：Android 12~12L 用 SCHEDULE_EXACT_ALARM，13+ 用 USE_EXACT_ALARM（提醒类应用专用，免动态申请） -->
    <uses-permission
        android:name="android.permission.SCHEDULE_EXACT_ALARM"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <!-- allowBackup=false：生日/姓名/备注属隐私数据，禁止通过备份通道导出 -->
    <application
        android:name=".BirthApp"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.BirthApp">

        <!-- singleTask：全局只允许一个主界面实例。否则从桌面小组件点进来会在
             旧界面上又叠一层新的，退出时得按好几次返回才能真正退到桌面 -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:theme="@style/Theme.BirthApp">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver
            android:name=".alarm.AlarmReceiver"
            android:exported="false" />

        <!-- exported=false：受保护系统广播照常投递，同时彻底屏蔽第三方应用访问 -->
        <receiver
            android:name=".alarm.BootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.TIME_SET" />
                <action android:name="android.intent.action.TIMEZONE_CHANGED" />
            </intent-filter>
        </receiver>

        <!-- 桌面小组件。同样 exported=false：系统是按组件名直接投递的，不需要对外开放 -->
        <receiver
            android:name=".widget.BirthWidgetReceiver"
            android:exported="false"
            android:label="@string/widget_label">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/birth_widget_info" />
        </receiver>

        <!-- 小组件配置页：首次把小组件拖到桌面时系统自动打开（birth_widget_info 的
             android:configure）。exported=true 是 configure 机制的硬性要求；
             excludeFromRecents 防止它出现在最近任务里（它是个一次性配置页） -->
        <activity
            android:name=".widget.WidgetConfigureActivity"
            android:excludeFromRecents="true"
            android:exported="true"
            android:theme="@style/Theme.BirthApp">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_CONFIGURE" />
            </intent-filter>
        </activity>

        <!-- 分享备份文件用。grantUriPermissions：接收方（微信/QQ 等）只在本次分享
             内拿到读权限，读不到白名单之外的任何文件 -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.birthapp.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>
